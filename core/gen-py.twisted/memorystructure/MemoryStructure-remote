#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.0-dev)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py:twisted
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import MemoryStructure
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  string saveWebEntity(WebEntity webEntity)'
  print '  string createCache( pageItems)'
  print '  i32 indexCache(string cacheId)'
  print '   getPrecisionExceptionsFromCache(string cacheId)'
  print '  void createWebEntities(string cacheId)'
  print '  void deleteCache(string cacheId)'
  print '  void markPageWithPrecisionException(string pageItemId)'
  print '  void saveWebEntityCreationRule(WebEntityCreationRule webEntityCreationRule)'
  print '  void savePageItems( pageItems)'
  print '  void saveNodeLinks( nodeLinks)'
  print '  void addLRUtoWebEntity(string id, PageItem pageItem)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = MemoryStructure.Client(protocol)
transport.open()

if cmd == 'saveWebEntity':
  if len(args) != 1:
    print 'saveWebEntity requires 1 args'
    sys.exit(1)
  pp.pprint(client.saveWebEntity(eval(args[0]),))

elif cmd == 'createCache':
  if len(args) != 1:
    print 'createCache requires 1 args'
    sys.exit(1)
  pp.pprint(client.createCache(eval(args[0]),))

elif cmd == 'indexCache':
  if len(args) != 1:
    print 'indexCache requires 1 args'
    sys.exit(1)
  pp.pprint(client.indexCache(args[0],))

elif cmd == 'getPrecisionExceptionsFromCache':
  if len(args) != 1:
    print 'getPrecisionExceptionsFromCache requires 1 args'
    sys.exit(1)
  pp.pprint(client.getPrecisionExceptionsFromCache(args[0],))

elif cmd == 'createWebEntities':
  if len(args) != 1:
    print 'createWebEntities requires 1 args'
    sys.exit(1)
  pp.pprint(client.createWebEntities(args[0],))

elif cmd == 'deleteCache':
  if len(args) != 1:
    print 'deleteCache requires 1 args'
    sys.exit(1)
  pp.pprint(client.deleteCache(args[0],))

elif cmd == 'markPageWithPrecisionException':
  if len(args) != 1:
    print 'markPageWithPrecisionException requires 1 args'
    sys.exit(1)
  pp.pprint(client.markPageWithPrecisionException(args[0],))

elif cmd == 'saveWebEntityCreationRule':
  if len(args) != 1:
    print 'saveWebEntityCreationRule requires 1 args'
    sys.exit(1)
  pp.pprint(client.saveWebEntityCreationRule(eval(args[0]),))

elif cmd == 'savePageItems':
  if len(args) != 1:
    print 'savePageItems requires 1 args'
    sys.exit(1)
  pp.pprint(client.savePageItems(eval(args[0]),))

elif cmd == 'saveNodeLinks':
  if len(args) != 1:
    print 'saveNodeLinks requires 1 args'
    sys.exit(1)
  pp.pprint(client.saveNodeLinks(eval(args[0]),))

elif cmd == 'addLRUtoWebEntity':
  if len(args) != 2:
    print 'addLRUtoWebEntity requires 2 args'
    sys.exit(1)
  pp.pprint(client.addLRUtoWebEntity(args[0],eval(args[1]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
